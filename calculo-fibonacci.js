// Inicializando as variáveis
let INDICE = 13;
let SOMA = 0;
let K = 0;

// Estrutura de repetição semelhante ao "Enquanto"
while (K < INDICE) {
    K = K + 1;
    SOMA = SOMA + K;
}

// Exibindo o valor final de SOMA
console.log("O valor final de SOMA é:", SOMA);
